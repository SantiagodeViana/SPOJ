#include <iostream>
using namespace std;

int mejor (int desde, int hasta){
    if (desde == hasta) return 0;
    for (int corte = desde + 1){
        corte < hasta;
        corte++
    }
    int hiq = mejorr (desde, corte)
    int hder = mejor(corte, hasta)
    int hnuevo = (hiza + hder + (pocion[desde][corte] + pocion[corte][desde]);
    if (hnuevo < mejor_humo){
       mejor_humo = hnuevo;
       pocion(desde)(hasta) = (pocion(desde)(corte) + pocion(corte)(hasta) % 100)
    }
}
return mejor_humo;
return 0;
}
