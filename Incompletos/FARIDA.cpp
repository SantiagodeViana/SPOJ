#include <iostream>
using namespace std;

int max_caramelo (int indice, bool puedo){
    if (indice == n) return 0;
    if (!puedo) return max_caramelo (indice +1, true);
    return dp [indice][puedo] (max_caramelo (indice +1, true));
           max_caramelo (indice +1, false) + caramelo [indice]);
int main (){

    return 0;
}
