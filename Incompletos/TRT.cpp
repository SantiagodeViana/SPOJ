int mentos (int inicio, int final, ind dia)
    if (dia == n) return 0;
    else{
         return (m[inicio*dia + mentos (inicial +1, final, dia+1);
                m[inicio*dia + mentos (inicial +1, final, dia+1);
                
